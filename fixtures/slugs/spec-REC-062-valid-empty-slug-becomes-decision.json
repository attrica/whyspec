{
  "rule": "REC-062",
  "statement": "An empty slug MUST become the literal `decision`.",
  "input_text": "?!?  ---  ***",
  "max_len": 60,
  "expected_slug": "decision",
  "note": "The source text contains NO characters in [a-z0-9], so the derived slug reduces to the empty string and MUST become the literal `decision`. An implementation returning '' produces the filename '<date>-.md'; one returning the raw text reintroduces unsafe characters."
}
