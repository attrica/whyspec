# Decision: Keep the current pipeline

**Status:** Accepted

## Context

Should the deploy pipeline change?

## Decision

The current pipeline is the only one the release window fits in.

## Alternatives considered

1. Blue-green deploy—rejected: no second environment exists
2. Keep the current pipeline — discarded: superseded by the new build
