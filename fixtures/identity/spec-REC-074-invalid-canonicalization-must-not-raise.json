{
  "input_path_description": "an absolute path containing a NUL (U+0000) byte",
  "input_path_escaped": "/records/\\u0000bad.md",
  "expected": "MUST NOT raise",
  "inputs": [
    "/records/\u0000bad.md",
    "Path with NUL",
    "decision",
    "Path with NUL"
  ],
  "joined_string": "/records/\u0000bad.md::Path with NUL::decision::path_with_nul",
  "expected_identity": "intent_2129502cb596",
  "rule": "REC-074",
  "statement": "A path outside a repository falls back to POSIX absolute; canonicalization MUST NOT raise.",
  "note": "A path containing a NUL byte. REC-074 states canonicalization MUST NOT raise, so a conformant implementation returns SOME string (or declines the path through its normal absent-value channel) rather than propagating an exception. DIVERGENCE: the reference implementation propagates ValueError('embedded null byte') from the underlying path resolution. Path spellings arrive from tool-call arguments and configuration, so this is a reachable trust boundary, not a theoretical one. The `expect` below states the SPEC-required outcome."
}
