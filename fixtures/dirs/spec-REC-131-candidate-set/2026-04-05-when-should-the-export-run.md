# Decision: Keep the nightly export window

**Status:** Accepted

## Context

When should the bulk export run?

## Decision

The nightly window is the only period with spare IO on the reporting replica.
