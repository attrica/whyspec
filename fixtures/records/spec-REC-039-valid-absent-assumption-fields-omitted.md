# Decision: Keep the export window at 24 hours

## Decision

The export window stays at 24 hours.

## Assumptions

- Nightly volume stays under the batch window (review by 2027-03-01).
- Downstream consumers tolerate a 24-hour lag.
