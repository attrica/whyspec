# ADR-0055 — Rely on the CDN's default TLS certificate

## Decision

Use the CDN's automatically issued certificate instead of a manually managed one.

## Assumptions

- The CDN keeps auto-renewing certificates (review by 12/01/2026).
