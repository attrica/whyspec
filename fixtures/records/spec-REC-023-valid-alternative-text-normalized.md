# Decision: Batch the nightly export

## Decision

The export runs once nightly in a single batch.

## Alternatives considered

1. **Streaming export**   — continuous,   higher operational cost.
2. Batch
   export, split across
   several lines.
