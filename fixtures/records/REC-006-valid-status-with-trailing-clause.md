# ADR-0048 — Rename the export CLI flag

**Status:** Rejected in favor of ADR-0049

## Decision

Do not rename `--out`; a later ADR supersedes this one with a different approach.

## Alternatives considered

1. Rename `--out` to `--output`. Rejected here (see ADR-0049 for the accepted shape).
