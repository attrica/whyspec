# Decision: Validate requests at the edge

**Status:** Accepted

## Context

Where should request validation live?

## Decision

Request validation runs at the edge handler. The template it replaces:

```markdown
## Alternatives considered
1. Validate in every service
```

That section is quoted, not ours.
