{
  "rule": "REC-066",
  "statement": "An empty question matches only a recorded context of `(none recorded)`.",
  "records_dir_files": {
    "keep-the-window.md": "# Decision: Keep the window\n\n## Context\n\n## Decision\n\nThe window stays.\n"
  },
  "chosen": "Keep the window",
  "question": "",
  "today": "2026-07-25",
  "expected_filename": "2026-07-25-keep-the-window.md",
  "note": "An EMPTY question against a legacy record whose `## Context` section exists but is EMPTY. REC-066 says an empty question matches a recorded context of `(none recorded)` AND ONLY THAT, so this MUST NOT match and a fresh dated record MUST be minted. DIVERGENCE: v0.8.3 compares whitespace-normalized strings first, so empty == empty matches and the legacy file IS claimed. The `expect` below states the SPEC-required outcome."
}
